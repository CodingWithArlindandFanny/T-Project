class Order {
    constructor(orderid, userid, orderdate, courses, totalAmount){
        this.orderid = orderid
        this.userid = userid
        this.orderdate = orderdate
        this.courses = courses
        this.totalAmount = totalAmount
    }
}